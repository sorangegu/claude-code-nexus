# AI 代理服务环境变量配置示例
# 复制此文件为 .env 并填入实际值

# GitHub OAuth 配置
# 在 https://github.com/settings/applications/new 创建新的 OAuth App
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# 加密密钥（用于加密存储的 API 密钥）
# 生成一个 32 字符的随机字符串
ENCRYPTION_KEY=your_32_character_encryption_key_here

# 应用基础 URL
# 开发环境
APP_BASE_URL=http://localhost:8787
# 生产环境示例
# APP_BASE_URL=https://your-domain.pages.dev

# 注意：
# 1. 这些环境变量需要在 wrangler.jsonc 的 vars 部分进行配置
# 2. 敏感信息（如 GITHUB_CLIENT_SECRET, ENCRYPTION_KEY）应该使用 
#    Cloudflare Workers 的 Secrets 功能存储，而不是直接放在 vars 中
# 3. 使用命令设置 secrets：
#    wrangler secret put GITHUB_CLIENT_SECRET
#    wrangler secret put ENCRYPTION_KEY
